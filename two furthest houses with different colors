class Solution:
    def maxDistance(self, a: List[int]) -> int:
        k = next(k for k in range(1,len(a)) if a[k-1]!=a[k])
        w = next(w for w in range(len(a)-2,-1,-1) if a[w]!=a[w+1])
        return max(a[0]!=a[-1] and len(a)-1,w,len(a)-k-1)
