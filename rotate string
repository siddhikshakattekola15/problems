class Solution:
    def rotateString(self, s: str, goal: str) -> bool:
        # Base condition
        if len(s) != len(goal):
            return False
        # append sequences
        return goal in (s + s)
